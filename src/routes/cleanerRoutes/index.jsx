import React from 'react';

// In the future, you can add specific pages and layouts for the cleaner role here.
// For now, an empty array is exported to prevent the application from crashing.

export const cleanerRoutes = [];
